{
  "name": "object-keys-mapping",
  "version": "1.3.0",
  "description": "mapping object keys",
  "main": "build/index.js",
  "scripts": {
    "build": "babel lib --out-dir build",
    "prepublish": "npm run build",
    "pretest": "npm run build",
    "test": "mocha --require babel/register",
    "test-cov": "istanbul cover node_modules/.bin/_mocha -- --require babel/register -R dot -t 2000 test/*.js"
  },
  "repository": "onebook/object-keys-mapping",
  "keywords": [
    "mapping",
    "object",
    "keys"
  ],
  "files": [
    "build/",
    "lib/"
  ],
  "author": "haoxin",
  "license": "MIT",
  "devDependencies": {
    "babel": "5",
    "istanbul": "0",
    "mocha": "2"
  }
}
